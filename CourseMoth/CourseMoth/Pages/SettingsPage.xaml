<?xml version="1.0" encoding="utf-8" ?>
<pages:CourseMothPage
    x:Class="CourseMoth.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:CourseMoth.Pages"
    xmlns:models="clr-namespace:CourseMoth.Models"
    xmlns:vm="clr-namespace:CourseMoth.ViewModels"
    x:DataType="vm:SettingsViewModel"
    Title="Settings">

    <ContentPage.Content>
        <ScrollView>
            <VerticalStackLayout
                BindableLayout.ItemsSource="{Binding Groups}"
                Padding="16,12,16,24"
                Spacing="20">

                <BindableLayout.ItemTemplate>
                    <DataTemplate x:DataType="models:SettingGroup">
                        <VerticalStackLayout Spacing="6">
                            <Label
                                FontAttributes="Bold"
                                FontSize="12"
                                Opacity="0.6"
                                Text="{Binding Name}" />

                            <Border Padding="12,4" StrokeShape="RoundRectangle 12">
                                <VerticalStackLayout BindableLayout.ItemsSource="{Binding Rows}">
                                    <BindableLayout.ItemTemplate>
                                        <DataTemplate x:DataType="models:SettingRow">
                                            <Grid
                                                Padding="0,10"
                                                ColumnDefinitions="*,Auto"
                                                ColumnSpacing="12"
                                                Opacity="{Binding Opacity}">
                                                <VerticalStackLayout Grid.Column="0" Spacing="2">
                                                    <Label Text="{Binding Label}" />
                                                    <Label
                                                        FontSize="11"
                                                        IsVisible="{Binding HasNote}"
                                                        Opacity="0.6"
                                                        Text="{Binding Note}" />
                                                </VerticalStackLayout>

                                                <Label
                                                    Grid.Column="1"
                                                    FontSize="13"
                                                    IsVisible="{Binding HasValue}"
                                                    Opacity="0.7"
                                                    Text="{Binding Value}"
                                                    VerticalOptions="Center" />
                                            </Grid>
                                        </DataTemplate>
                                    </BindableLayout.ItemTemplate>
                                </VerticalStackLayout>
                            </Border>
                        </VerticalStackLayout>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </VerticalStackLayout>
        </ScrollView>
    </ContentPage.Content>
</pages:CourseMothPage>
